package com.example.serverZHDE.repositories;

import com.example.serverZHDE.entities.NearStation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NearStationRepository extends JpaRepository<NearStation, Long> {
}
